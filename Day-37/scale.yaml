apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: app-autoscaler

spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: advanced-app

  minReplicas: 2
  maxReplicas: 5

  targetCPUUtilizationPercentage: 50
