groups:
  - name: cpu-alert
    rules:
      - alert: HighCPUUsage
        expr: rate(node_cpu_seconds_total[1m]) > 0.8
        for: 1m
        labels:
          severity: warning
        annotations:
          description: "High CPU usage detected"
